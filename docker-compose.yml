version: "3"
services:
  app:
    image: nibo
    build:
      context: .
    ports:
      - 5000:80
      - 5001:443
    networks:
      - nibo-network
  #command: ["dotnet", "MyWebApp.dll"]
networks:
  nibo-network:
    driver: bridge
